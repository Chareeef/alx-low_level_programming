#include "main.h"
#include <stdio.h> 
#include <limits.h>

/**
 * main - Entry point
 *
 * Return: 0 on Succes.
 */
int main(void)
{
	char k[] = "abawa";
	
	printf("%s", k);
	puts_half(k);
	return (0);
}


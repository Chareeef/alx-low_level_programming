#include "main.h"
#include <stdio.h> 
#include <limits.h>

/**
 * main - Entry point
 *
 * Return: 0 on Succes.
 */
int main(void)
{
	char k[] = "abawas!";
	
	printf("%s", k);
	puts2(k);
	return (0);
}

